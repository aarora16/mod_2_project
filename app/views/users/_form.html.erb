<%= form_for @user do |f| %>  
  <div class="label"><%= f.label "Type of Car: " %></div><br>
  <div class="field"><%= f.select :type_of_car, ["Exotic", "SUV", "Sports", "Family", "Sedan", "Luxury", "Economical"] %></div><br>

  <div class="action"><%= f.submit %></div>
<% end %>

<br>

<% if @user.errors.any? %>
  <div id="error_explanation">
    <strong><%= pluralize(@user.errors.count, "error") %>
      prohibited this article from being saved:</strong>
    
    <% @user.errors.full_messages.each do |msg| %>
      <div id="error_message">
        <ul><%= msg %></ul>
      </div>
    <% end %>
  </div>  
<% end %>